셰이더 모델 5.0기준 상수 버퍼 하나의 최대 크기는 64KB(4096 레지스터).
상수 버퍼의 기본 메모리 접근(패킹) 단위는 16byte - GPU는 병렬 계산 구조라서 한번에 읽어 들이는 메모리 규격이 큼
이로인해서 상수 버퍼에 배열 문법을 쓰게되면 각 배열 칸마다 16byte로 읽어냄(메모리 문제 발생)

본 애니메이션 매트릭스나 수만 개의 파티클 좌표처럼 64KB를 초과하거나 배열 형태가 필요한 큰 메모리는 상수 버퍼 대신 StructuredBuffer (DX11의 SRV, Unity의 ComputeBuffer)를 사용